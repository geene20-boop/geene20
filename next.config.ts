import type { NextConfig } from "next";

// Force rebuild timestamp: 2026-08-05 18:35:00
// Clear all build caches - fresh rebuild required
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
