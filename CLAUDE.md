@AGENTS.md

# 배포 시 업데이트 안내 팝업 (changelog.ts)

직원용 화면에는 접속 시 자동으로 뜨는 "업데이트 안내" 팝업이 있다 (`src/components/NoticeModal.tsx`).
이 팝업은 `src/lib/changelog.ts`의 `CHANGELOG` 배열 맨 앞 항목(`CURRENT_VERSION`)을 보여주며,
직원 브라우저가 마지막으로 확인한 버전과 다르면 자동으로 노출된다.

**사용자에게 눈에 띄는 변경사항을 만들고 PR을 올릴 때마다, 별도로 요청받지 않아도 항상**
`src/lib/changelog.ts`의 `CHANGELOG` 배열 맨 앞에 새 항목을 추가한다 (기존 항목은 그대로 둔다):

```ts
{
  version: "YYYY.MM.DD", // 오늘 날짜, 같은 날 여러 건이면 마지막에 합쳐서 추가
  date: "YYYY-MM-DD",
  items: [
    { title: "짧은 기능명", desc: "직원이 이해할 수 있는 한 줄 설명 (화면 문구·클릭 방법 기준, 코드/파일명 언급 금지)" },
    // ...변경 건수만큼
  ],
},
```

- 대상: 화면·동작이 바뀌어 직원이 알아야 하는 변경만 포함한다 (내부 리팩터링, 버그 재발 방지용 테스트 추가 등 화면에 안 보이는 변경은 제외).
- 문구는 이 세션에서 만든 기존 보고서/공지 스타일처럼 사용자 관점으로 쓴다 ("~가 자동으로 채워집니다" 등), 기술 용어는 피한다.
- 같은 날 여러 PR이 나가면 그날 날짜의 기존 항목에 items만 추가한다 (버전 중복 생성 금지).

